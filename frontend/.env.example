# Leave empty to use same-origin /api through Nginx.
# Local development can use http://localhost:8000.
# Do not set this to http://localhost:8000/api.
NEXT_PUBLIC_API_BASE_URL=

# Optional build-time override. Defaults to the version in package.json.
NEXT_PUBLIC_APP_VERSION=
