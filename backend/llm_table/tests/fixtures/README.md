# LLM table fixtures

Generated LLM output snapshots are intentionally not kept in Git. Put any local
snapshot under `llm_output/`; the repository ignores that directory. Reusable,
small fixtures should be committed next to the test that consumes them.
